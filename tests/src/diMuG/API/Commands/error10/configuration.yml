# types of artefact's, which are served by this api
types:
  coins:
    # short human readable name
    label: 'Münzen'
    fields:
      test:
        label: 'test'
        type: 'string'
    finder:
      clas: 'diMuG\Test\FactoryMock'
      factory: true
      method: 'getFinder'
      static: false